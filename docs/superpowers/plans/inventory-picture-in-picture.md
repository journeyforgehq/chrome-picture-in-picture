# Inventory — scaffolded picture-in-picture child, before any edit

Built by reading the code at commit 2a57320 (branch `feat/v1-free`), not from any
task description. Every row was confirmed by opening the file named in the last
column. No keep/drop decision is recorded here on purpose — decisions are a
separate, later task, so this list is written without an eye on what would be
convenient to drop.

Paths are relative to the repo root unless they start with `src/`, `test/`,
`e2e/`, `webpack/` or `preview/`, which are relative to `extension/`.

| # | Artifact | Capability | Where it is exercised |
|---|---|---|---|
| 1 | popup | Renders the tier badge (Free/Pro antd Tag) from the resolved tier | src/popup/PopupView.tsx:88, src/ui-kit/TierBadge.tsx, test/popup/PopupView.test.tsx, e2e/popup-loads.spec.ts, e2e/billing.spec.ts, e2e/grace.spec.ts, e2e/dunning-refund.spec.ts, e2e/renewal-cascade.spec.ts, e2e/restore.spec.ts |
| 2 | popup | Paywall trigger: the locked pro feature's "Unlock" button calls `onOpenPaywall`, which opens `UpgradePaywall` | src/popup/PopupView.tsx:101-103, src/ui-kit/LockedFeature.tsx:48, test/popup/PopupView.test.tsx ("clicking Unlock … calls onOpenPaywall"), test/popup/popup.test.tsx |
| 3 | popup | Checkout hand-off: `onCheckout(planId)` → `checkoutUrl(planId, deviceId)` → `chrome.tabs.create({url})`, falling back to `window.open` when `chrome.tabs` is absent | src/popup/popup.tsx:58-65, src/billing/checkout.ts, test/popup/popup.test.tsx ("onCheckout opens the checkout URL via chrome.tabs.create", asserts `client_reference_id=device-abc-123`) |
| 4 | popup | Mounts `UpgradePaywall` with the shared `PLANS` array — one of only two production mounts of the paywall (the other is options) | src/popup/PopupView.tsx:105-110, src/popup/popup.tsx:77, test/popup/PopupView.test.tsx (open/closed dialog cases) |
| 5 | popup | Renders `PaymentNudge` (the `past_due` "Payment issue" alert + "Update payment method" link) at the top of the popup body | src/popup/PopupView.tsx:91, src/ui-kit/PaymentNudge.tsx, **e2e/dunning-refund.spec.ts:60-61 asserts this ON THE POPUP page** |
| 6 | popup | Accepts a `plan` prop (`PopupViewProps.plan`) that is passed by the container but **never rendered** — there is no plan badge in the popup; `PlanBadge` is mounted only by OptionsView | src/popup/PopupView.tsx:16 + :72-81 (destructuring omits `plan`), src/popup/popup.tsx:71 |
| 7 | popup | `UppercaseTool` — the always-free demo feature (labelled input "Text to uppercase", "Uppercase" button, `data-testid="uppercase-result"`). Exists nowhere else in the product | src/popup/PopupView.tsx:27-43, test/popup/PopupView.test.tsx ("the always-free Uppercase tool works regardless of tier") |
| 8 | popup | `ProTool` — the pro-gated demo feature (reverse text, "Run pro tool" button, `data-testid="pro-tool-result"`). The `run pro tool` button enabled/disabled state is the Layer-2 pro assertion in 4 e2e specs | src/popup/PopupView.tsx:46-64, e2e/billing.spec.ts, e2e/grace.spec.ts, e2e/dunning-refund.spec.ts, e2e/renewal-cascade.spec.ts |
| 9 | popup | Sole production mount of `LockedFeature` — disabled `<fieldset>` at `opacity: 0.5` + `.ui-kit-locked-feature` overlay. `.ui-kit-locked-feature fieldset` is the CSS selector 5 e2e specs assert against | src/popup/PopupView.tsx:101, src/ui-kit/LockedFeature.tsx, e2e/popup-loads.spec.ts, e2e/billing.spec.ts, e2e/dunning-refund.spec.ts, e2e/renewal-cascade.spec.ts |
| 10 | popup | Device-id bootstrap on mount: `getDeviceId(chromeSyncLocalStores())` writes `chrome.storage.local.device_id` — this write is what `waitForDeviceId` polls for in 6 specs | src/popup/popup.tsx:21-23, src/billing/device-id.ts, e2e/harness/identity.ts:29 |
| 11 | popup | Entitlement lifecycle: `createEntitlement(...)` → `getCached()` seed → `refresh()` → re-read plan/status, with a `cancelled` guard on unmount | src/popup/popup.tsx:17-56, test/popup/popup.test.tsx |
| 12 | popup | "No Free flash": a cached Pro record is painted before the network resolves. Regression-tested for the popup only; options.tsx has the same code but **no equivalent test** | src/popup/popup.tsx:36-41, test/popup/popup.test.tsx ("seeds the cached Pro tier immediately (no Free flash)") |
| 13 | popup | `ThemeProvider accent={config.ACCENT}` wraps the popup root (antd theme token wiring) | src/popup/popup.tsx:68, src/ui-kit/ThemeProvider.tsx, src/ui-kit/theme.ts |
| 14 | popup | Fixed 360px MV3-natural width with 16px padding, so the same component mounts unmodified in the preview gallery | src/popup/PopupView.tsx:83, preview/gallery.tsx:52-66 |
| 15 | popup | HTML shell: `popup.html` with `<title>Reference Extension</title>`, `#root`, and `index.tsx` that `createRoot`s and throws "popup root element missing" if the node is absent | src/popup/popup.html, src/popup/index.tsx |
| 16 | popup | Heading text "Reference Extension" in the popup title row | src/popup/PopupView.tsx:85-87 |
| 17 | popup | Mounted twice in the dev preview gallery (`popupview-free`, `popupview-pro` cards), each with independent local paywall state | preview/gallery.tsx:19, :52-66, :148-156; test/webpack-preview-config.test.ts |
| 18 | e2e — popup entry point | `e2e/popup-loads.spec.ts` — navigates `popup.html`; asserts Free badge visible, locked fieldset `opacity: 0.5` and `disabled === true` | e2e/popup-loads.spec.ts |
| 19 | e2e — popup entry point | `e2e/identity.spec.ts` — navigates `popup.html`; asserts a fresh install generates a UUID device id and caches tier `free` | e2e/identity.spec.ts:6 |
| 20 | e2e — popup entry point | `e2e/billing.spec.ts` — navigates `popup.html`; full money loop (free/locked → webhook grant → Pro/unlocked → cancel webhook → Free/re-locked); writes `popup-free.png` and `popup-pro.png`; then checks the options page at 375px width and writes `options-mobile.png`. **CORE-vendored file** (drift-guarded) | e2e/billing.spec.ts:19, :28, :35, :51-56, :67-69, :72-76 |
| 21 | e2e — popup entry point | `e2e/grace.spec.ts` — navigates `popup.html`; grants Pro, aborts every `**/me` request, reloads and asserts Pro survives (fresh-cache half of the 7-day offline grace); writes `grace-pro.png` | e2e/grace.spec.ts:14, :27 |
| 22 | e2e — popup entry point | `e2e/dunning-refund.spec.ts` — navigates `popup.html`; asserts `past_due` keeps Pro *and* shows the nudge, that webhook redelivery is graceful, and that `charge.refunded` re-locks; writes `dunning-nudge.png`, `refund-relocked.png` | e2e/dunning-refund.spec.ts:20, :28 |
| 23 | e2e — popup entry point | `e2e/renewal-cascade.spec.ts` — navigates `popup.html` on **two independent installs**; asserts distinct device ids, both granted under one customer, renewal keeps both Pro, cancel re-locks both; writes `cascade-a.png`, `cascade-b.png` | e2e/renewal-cascade.spec.ts:20, :32, :36 |
| 24 | e2e — popup entry point | `e2e/restore.spec.ts` — device A navigates `popup.html` (device-id read + Pro assertion); device B uses `options.html` for the actual restore flow | e2e/restore.spec.ts:15, :27, :44-46 |
| 25 | e2e — popup artifact | `e2e/health.spec.ts` — asserts `dist/popup.html` exists on disk after globalSetup's build (file existence, not navigation) | e2e/health.spec.ts:9 |
| 26 | e2e — docs | `e2e/README.md` lists the hermetic spec set including "`popup-loads.spec.ts` — popup Free, pro tool locked" | e2e/README.md:12 |
| 27 | e2e harness | `harness/identity.ts` has **no hard-coded popup URL** — each spec builds its own `popupUrl(id)`. Its contract is "must be called on an extension page (popup.html/options.html)", and `waitForDeviceId`'s doc comment explains the race by citing `src/popup/popup.tsx`'s `useState<Tier>("free")` by name | e2e/harness/identity.ts:9-11, :22-28 |
| 28 | e2e harness | `readDeviceId` / `waitForDeviceId` / `readCachedTier` read `chrome.storage.local` keys `device_id` and `entitlement_cache`. **CORE-vendored file** (drift-guarded) | e2e/harness/identity.ts |
| 29 | e2e harness | Per-test fresh Chrome-for-Testing persistent context with the unpacked `dist/` loaded; extension id derived from the MV3 service-worker URL host | e2e/harness/extension.ts, e2e/fixtures.ts |
| 30 | e2e harness | Signed Stripe webhook forgery (`checkout.session.completed`, `customer.subscription.deleted/updated`, `invoice.payment_failed/paid`, `charge.refunded`) posted to `/stripe/webhook` | e2e/harness/webhook.ts, test/harness/webhook.test.ts |
| 31 | e2e harness | `globalSetup` builds `dist/` with `BACKEND_BASE_URL` baked to the target, then spawns a wiped local `wrangler dev` (or health-checks + probes a deployed staging worker) | e2e/global-setup.ts, e2e/harness/wrangler.ts, e2e/harness/config.ts |
| 32 | e2e config | `playwright.config.ts`: `testDir: ./e2e`, `testMatch: **/*.spec.ts`, `workers: 1`, `fullyParallel: false` — every `*.spec.ts` under `e2e/` is auto-collected | extension/playwright.config.ts |
| 33 | content script | Exports `MARKER_ATTR` and sets a presence marker attribute on `document.documentElement`; idempotent across repeat calls | src/content/content.ts:12-17, test/content.test.ts |
| 34 | content script | The `__slug__` token is baked into the attribute name: `data-picture-in-picture-present`. `test/content.test.ts` asserts that exact literal ("uses the picture-in-picture scaffold token in the attribute name") | src/content/content.ts:12, test/content.test.ts:14-16 |
| 35 | content script | Runtime entry is guarded by `typeof document !== "undefined"` so the module can be imported under vitest/happy-dom | src/content/content.ts:21-23 |
| 36 | content script | Is the declared subject of the separation guard: header comment states it must NEVER import antd, `@ant-design/*`, or anything under `ui-kit/` | src/content/content.ts:6-10, webpack/separation-guard.cjs |
| 37 | manifest | MV3 (`manifest_version: 3`), name/description "Picture in Picture - Floating Video Player", version 0.1.0; the name literal is asserted as the scaffold token | src/static/manifest.json:1-5, test/manifest.test.ts:41-43 |
| 38 | manifest | `permissions: ["storage","activeTab"]` — asserted with `toEqual`, i.e. an **exact** array match (adding or removing one permission fails the test) | src/static/manifest.json:6, test/manifest.test.ts:15 |
| 39 | manifest | `activeTab` is declared but **no code consumes it** — there is no `chrome.tabs.query`, `chrome.scripting`, or `executeScript` call anywhere in `src/` | src/static/manifest.json:6 (grep of src/ finds no consumer) |
| 40 | manifest | Background service worker declared as `{service_worker: "background.js", type: "module"}` — asserted with `toEqual` | src/static/manifest.json:7-10, test/manifest.test.ts:18-23 |
| 41 | manifest | Exactly one `content_scripts` entry: `js: ["content.js"]`, `run_at: "document_idle"`, non-empty `matches` array | src/static/manifest.json:11-17, test/manifest.test.ts:25-33 |
| 42 | manifest | Narrow match: `matches` is asserted BOTH as "not containing `<all_urls>`" AND as exactly `["https://example.com/*"]` — the literal placeholder host is pinned by the test | src/static/manifest.json:13, test/manifest.test.ts:35-39 |
| 43 | manifest | `action` = `{default_popup: "popup.html", default_icon: {16,48,128}}` — asserted with `toEqual`, so removing `default_popup` (or the whole `action`) fails `test/manifest.test.ts` | src/static/manifest.json:18-25, test/manifest.test.ts:45-53 |
| 44 | manifest | No `chrome.action.onClicked` listener exists anywhere — the toolbar click is entirely handled by `default_popup` today | src/background/background.ts (no `chrome.action` reference), grep of src/ |
| 45 | manifest | `options_page: "options.html"` and a top-level `icons` 16/48/128 triple, both asserted | src/static/manifest.json:26-31, test/manifest.test.ts:54-59 |
| 46 | manifest | No `host_permissions` at all; cross-origin `/me` works only because the worker returns `Access-Control-Allow-Origin: *` | src/static/manifest.json, e2e/README.md:30-31 |
| 47 | webpack | Four entries: `background`, `content`, `popup` (`src/popup/index.tsx`), `options` (`src/options/index.tsx`) | webpack/webpack.common.cjs:26-31 |
| 48 | webpack | `HtmlWebpackPlugin` for the popup: template `src/popup/popup.html`, filename `popup.html`, `chunks: ["popup"]` (and the matching options instance) | webpack/webpack.common.cjs:63-72 |
| 49 | webpack | `test/webpack-config.test.ts` asserts the prod build emits a `popup` chunk, an `options` chunk, and both `popup.html` and `options.html` assets | test/webpack-config.test.ts:43-55 |
| 50 | webpack | `CopyPlugin` copies `src/static` (manifest.json + icons) verbatim into `dist/` | webpack/webpack.common.cjs:62 |
| 51 | webpack | `DefinePlugin` injects 8 public build vars: `BACKEND_BASE_URL`, `STRIPE_MONTHLY_URL`, `STRIPE_ANNUAL_URL`, `STRIPE_LIFETIME_URL`, `WELCOME_URL`, `UNINSTALL_URL`, `ACCENT`, `DEV_PRO` | webpack/webpack.common.cjs:52-61, test/config.test.ts, test/webpack-config.test.ts:70-98 |
| 52 | webpack | `APP_ENV` selects the committed env file (`.env` / `.env.staging` / `.env.production`), then `.env.local` layers on top; shell env always wins | webpack/webpack.common.cjs:17-23, package.json `build:staging` / `build:prod` / `start:*` |
| 53 | webpack | `webpack.prod.cjs` hard-pins `DEV_PRO` to `"false"` regardless of the shell, plus `bail: true`; `webpack.dev.cjs` allows the shell toggle and inline source maps | webpack/webpack.prod.cjs, webpack/webpack.dev.cjs, test/webpack-config.test.ts:30-41 |
| 54 | webpack | `splitChunks: false` and `output.clean` — every entry is a single self-contained file, which is what makes the per-chunk separation guard meaningful | webpack/webpack.common.cjs:48-49 |
| 55 | webpack | `webpack.preview.cjs` — a separate dev-only build of `preview/gallery.tsx` into `dist-preview/`, deliberately NOT merged into common so antd never enters the extension build graph | webpack/webpack.preview.cjs, test/webpack-preview-config.test.ts, package.json `preview:build` / `preview:serve` |
| 56 | separation guard | Forbids, **in the compiled chunk named `content` only**, any module whose resolved path matches `/[\\/]ui-kit[\\/]/`, `node_modules/antd`, or `node_modules/@ant-design/` | webpack/separation-guard.cjs:7-21 |
| 57 | separation guard | Walks webpack's real module graph (`compilation.chunkGraph.getChunkModulesIterable`), so **transitive** leaks are caught, not just direct imports | webpack/separation-guard.cjs:13-21, test/separation-fixture/content-transitive.ts |
| 58 | separation guard | Reports the offending resolved paths, pushes onto `compilation.errors` **and** throws from the `afterEmit` tap so `compiler.run()`'s promise rejects deterministically | webpack/separation-guard.cjs:23-48 |
| 59 | separation guard | Silently no-ops when no chunk is named `content` (`if (!contentChunk) return;`) — renaming or removing the content entry disables the guard without any signal | webpack/separation-guard.cjs:10-11 |
| 60 | separation guard | Two-sided test coverage: the real prod build's content chunk stays antd-free, AND a fixture whose content entry transitively imports ui-kit must fail with `/separation-guard/i` | test/separation.test.ts, test/separation-fixture/webpack.fixture.cjs |
| 61 | PLANS | Three-tier ladder: `monthly` $3.99 `/mo`, `annual` $29 `/yr` with `highlight: true`, `lifetime` $79 `once` — each with `priceNote`, `description`, and a 3-item `features` bullet list | src/billing/plans.ts:19-47 |
| 62 | PLANS | Single source imported by **both** `src/popup/popup.tsx` and `src/options/options.tsx` | src/billing/plans.ts:9, src/popup/popup.tsx:6, src/options/options.tsx:6 |
| 63 | PLANS | `plans.test.ts` non-domination rules: at least one plan, each with label+price; `lifetime > annual`; `annual < monthly × 12`; a highlighted `annual` must not be dominated by `lifetime`. All three ladder rules are **conditional on the ids being present**, so a single-plan array satisfies them vacuously | test/plans.test.ts:9-43 |
| 64 | PLANS | Plan `id` maps to `config.STRIPE_LINKS[id]`, i.e. to the `STRIPE_MONTHLY_URL` / `STRIPE_ANNUAL_URL` / `STRIPE_LIFETIME_URL` env vars, and `checkoutUrl` appends `client_reference_id=<deviceId>` | src/billing/config.ts:37-41, src/billing/checkout.ts, test/checkout.test.ts, extension/.env, extension/.env.example |
| 65 | PLANS | `UpgradePaywall` layout is driven by `plans.length`: col span 8/12/24 and modal width 780/560/380 — one plan renders as a single 380px card | src/ui-kit/UpgradePaywall.tsx:59, :76 |
| 66 | PLANS | The repo-root preflight fails shipping if **both** `STRIPE_ANNUAL_URL` and `STRIPE_LIFETIME_URL` are empty ("checkout has no link") — `STRIPE_MONTHLY_URL` alone does not satisfy it | scripts/preflight.mjs:18-20, test/preflight.test.mjs |
| 67 | PLANS | `src/billing/plans.ts` is a **CORE-vendored file** with a recorded SHA256 in `.factory.json`; local edits are reported as drift and refused by `sync-core` without `--force`. The same applies to `e2e/billing.spec.ts`, `e2e/fixtures.ts`, `e2e/global-setup.ts`, `e2e/harness/*.ts`, `src/ui-kit/*`, `src/billing/*`, `src/contract.ts` | .factory.json (`core` map), file header banners |
| 68 | PLANS | The backend derives the displayed plan label from the Stripe checkout **mode**, not from the clicked plan id: `mode === "subscription" → "annual"`, otherwise `"lifetime"`; `periodEnd` is null only for lifetime | backend/src/billing/webhook.ts:45, :80 |
| 69 | OptionsView | Heading "Reference Extension — Settings"; `e2e/billing.spec.ts` asserts `/Settings/` is visible after loading the options page at 375×667 | src/options/OptionsView.tsx:57, e2e/billing.spec.ts:72-76 |
| 70 | OptionsView | Restore form: labelled "Email" input + "Restore purchase" submit, with antd `required`+`type: email` validation | src/options/OptionsView.tsx:73, src/ui-kit/RestoreForm.tsx:62-75, test/ui-kit/RestoreForm.test.tsx, test/options/OptionsView.test.tsx, e2e/restore.spec.ts |
| 71 | OptionsView | Restore status messages, all four branches: success "Purchase restored — you're Pro again.", 404-or-200 "No active purchase found for that email", 429 "Too many attempts, try again later", generic error fallback — each with `role="alert"` | src/ui-kit/RestoreForm.tsx:21-47, test/ui-kit/RestoreForm.test.tsx, e2e/restore.spec.ts:65-67, :98-100 |
| 72 | OptionsView | Restore container wiring: `entitlement.restore(email)` with a `restoring` loading/disabled state, result stored in state, tier updated from the result | src/options/options.tsx:61-71, test/options/options.test.tsx |
| 73 | OptionsView | Payment nudge (`PaymentNudge`) rendered above the plan badge | src/options/OptionsView.tsx:63, test/ui-kit/PaymentNudge.test.tsx |
| 74 | OptionsView | Plan badge (`PlanBadge`): "Monthly"/"Annual"/"Lifetime" tag plus a status tag ("Active"/"Inactive"/"Canceled"/"Past due"), or "No plan" when `plan` is undefined | src/options/OptionsView.tsx:64, src/ui-kit/PlanBadge.tsx, test/ui-kit/PlanBadge.test.tsx, test/options/OptionsView.test.tsx, e2e/restore.spec.ts:58, :72-76, :92 |
| 75 | OptionsView | Paywall: a primary "Upgrade" button calls `onOpenPaywall`, and `UpgradePaywall` is mounted with the shared `PLANS` | src/options/OptionsView.tsx:65-67, :75-80, test/options/OptionsView.test.tsx:67-76 |
| 76 | OptionsView | Checkout hand-off identical to the popup's: `checkoutUrl(planId, deviceId)` opened via `chrome.tabs.create`, `window.open` fallback | src/options/options.tsx:73-80 |
| 77 | OptionsView | Same entitlement lifecycle as the popup (device id → client → cached seed → refresh → plan/status), and it is the options page (not the popup) that `restore.spec.ts` uses to bootstrap device B's identity | src/options/options.tsx:21-59, e2e/restore.spec.ts:51-55 |
| 78 | OptionsView | Layout: 480px max-width bordered card centred with 48px margin; verified responsive at 375px width in e2e (`options-mobile.png`) | src/options/OptionsView.tsx:48-56, e2e/billing.spec.ts:72-77 |
| 79 | OptionsView | Mounted in the preview gallery in two states (`optionsview-free`, `optionsview-restore-404`) | preview/gallery.tsx:20, :68-85, :158-166 |
| 80 | OptionsView | HTML shell `options.html` with `<title>Reference Extension — Settings</title>` and `index.tsx` that throws "options root element missing" | src/options/options.html, src/options/index.tsx |
| 81 | ui-kit | `UpgradePaywall` behaviours beyond plan cards: `destroyOnClose`, `focusTriggerAfterClose={false}` (so a focus-to-upsell gate can't re-trap the user), a "POPULAR" ribbon for the highlighted plan, `Choose {label}` default CTA overridable via `ctaLabel`, and description/priceNote rows reserved only when some plan uses them | src/ui-kit/UpgradePaywall.tsx:51-183, test/ui-kit/UpgradePaywall.test.tsx |
| 82 | ui-kit | `ThemeProvider` / `buildTheme(accent)` shared antd theme for every extension-owned page | src/ui-kit/ThemeProvider.tsx, src/ui-kit/theme.ts, test/ui-kit/theme.test.tsx |
| 83 | ui-kit | Barrel `src/ui-kit/index.ts` re-exports all seven components plus their prop types; `test/ui-kit/index.test.ts` asserts the export surface | src/ui-kit/index.ts, test/ui-kit/index.test.ts |
| 84 | preview gallery | Renders every ui-kit component in every representative state (TierBadge ×2, PlanBadge ×3, PaymentNudge, LockedFeature locked/unlocked, UpgradePaywall open-by-default, RestoreForm idle/success/404/429) for screenshot + computed-style verification | preview/gallery.tsx:87-170 |
| 85 | background | `handleInstalled` opens `WELCOME_URL` exactly once, only on `reason === "install"`, only when configured | src/background/background.ts:13-21, test/background.test.ts |
| 86 | background | `uninstallUrl` registers the post-uninstall feedback page with `?v=<manifest version>` appended, returns null on empty/malformed URL, and is re-set on every SW startup. Privacy: version only, never the deviceId | src/background/background.ts:33-42, :67-71, test/background.test.ts |
| 87 | background | `handleMessage` runtime-message relay stub returning `{handled: false}`, wired to `chrome.runtime.onMessage` with a synchronous `sendResponse` | src/background/background.ts:58-60, :79-83 |
| 88 | billing core | Device id: `crypto.randomUUID()` persisted to `chrome.storage.sync` with a `chrome.storage.local` mirror under key `device_id` | src/billing/device-id.ts, src/billing/chrome-storage.ts, test/device-id.test.ts, test/billing/chrome-storage.test.ts |
| 89 | billing core | Entitlement client: `GET /me` with `X-Device-Id`, cached at `entitlement_cache` (`{tier, plan, status, checkedAt}`), 7-day offline grace, `restore(email)`, `clear()`, and a `devPro` short-circuit | src/billing/entitlement.ts, test/entitlement.test.ts, src/billing/entitlement.test.ts, e2e/grace.spec.ts |
| 90 | billing core | `src/contract.ts` must stay **byte-identical** to `backend/src/contract.ts` (`Tier`, `Plan`, `PaidStatus`, `MeResponse`, `RestoreRequest/Response`, `ERROR_CATALOG`, `errorFor`) | src/contract.ts, test/contract-sync.test.ts |
| 91 | tooling | `package.json` scripts asserted by test: `build`, `build:dev`, `test`, `typecheck`, `verify` must have exact literal values | extension/package.json, test/scripts.test.ts |
| 92 | tooling | `gen-icons.mjs` regenerates the 16/48/128 PNG triple; `icons.test.ts` runs it and validates PNG signature + IHDR dimensions | extension/scripts/gen-icons.mjs, test/icons.test.ts |
| 93 | tooling | `build:zip` packages the **contents** of `dist/` (manifest.json at archive root) for CWS upload | extension/scripts/zip-dist.mjs, package.json |
| 94 | welcome page | Shipped onboarding copy hard-codes the popup as step 1 of "How to use": "Open the popup — Click the Hello Gated icon in your toolbar", plus feature copy for "Uppercase" (free) and "Reverse text" (pro), appName "Hello Gated", and permission justifications for `storage` and `activeTab` | welcome-page/src/content.ts:17-33 |
| 95 | welcome page | `pro.restoreHref` still contains `chrome-extension://REPLACE_WITH_EXTENSION_ID/options.html`, which the repo-root preflight flags as an unfilled placeholder | welcome-page/src/content.ts:40, scripts/preflight.mjs:13-15 |
