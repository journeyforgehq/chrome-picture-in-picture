node_modules/
dist/
dist-fixture/
dist-preview/
*.log
.wrangler-e2e/
test-results/
playwright-report/
e2e/__screens__/
.env.local
extension.zip
.tmp-injected-bundle/
.tmp-zip-dist/
.tmp-zip-dist.zip
# Private prod bundle for test/background/registration-wiring.test.ts (it must
# not read the shared dist/ — see that file's OUT_DIR comment).
.tmp-registration-wiring/
# dist/ with <all_urls> promoted to a required host permission, built by
# playwright.granted.config.ts's globalSetup. NEVER a shippable artifact —
# see e2e/granted-dist.ts.
.tmp-granted-dist/
# Private prod bundle for the __ORG__ submission gate in
# test/options/options.test.tsx — must not read the shared dist/, same
# reason as .tmp-injected-bundle/ above.
.tmp-options-gate/
