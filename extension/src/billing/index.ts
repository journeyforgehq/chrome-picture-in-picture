// =============================================================================
// GENERATED - do not edit here. CORE file vendored from chrome-ext-factory.
// Edit upstream in the template and run `sync-core` to propagate. Local edits
// are reported as drift and refused without --force.
// =============================================================================
export { getDeviceId } from "./device-id";
export type { StorageArea, DeviceIdStores } from "./device-id";

export { config } from "./config";
export type { Config, StripeLinks } from "./config";

export { createEntitlement } from "./entitlement";
export type {
  Entitlement,
  EntitlementCache,
  RestoreResult,
  CreateEntitlementOptions,
} from "./entitlement";

export { checkoutUrl } from "./checkout";

export { fetchPortalUrl } from "./portal";
